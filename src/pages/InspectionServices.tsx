import { Container } from "react-bootstrap";

export function InspectionServices(){
    return (
        <Container className="default">
            We do inspection services by helping client's get actual cargo pictures before loading from factories or ports anywhere in Taiwan for US$100 / loading site / 100MT . Each inspected products will be printed on and with its own unique serial number and marking.
        </Container>
    )
}